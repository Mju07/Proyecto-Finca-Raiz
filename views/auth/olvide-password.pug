extends ../layout/index

block content
    .py-5
        h1.text-center.text-5xl.my-5.font-extrabold.text-black Bien
            span.font-normal Raíz
        h2.text-center.text-3xl.font-extrabold.text-black= pagina

        if errores
            div(class="max-w-md mx-auto my-10")
                each error in errores
                    p.text-red-600.font-bold.uppercase.text-xs.text-center.p-1= error.msg

        .mt-8.mx-auto.max-w-lg
            .bg-indigo-100.py-8.px-6.shadow-md.rounded-lg
                form(method="POST")
                    input(type="hidden" name="_csrf" value= csrfToken)
                    div.mb-6
                        label.block.text-sm.uppercase.text-neutral-950.mb-1.font-bold(for="email") Correo Electrónico
                        input#email.w-full.py-2.px-3.placeholder-gray-700.bg-white.rounded-lg( placeholder="Ingresa tu Correo Electrónico"  name="email" type="email" value= usuario ? usuario.email : '')
                    input.w-full.bg-indigo-400.text-black.font-bold.py-3.cursor-pointer.uppercase.text-sm.rounded-md(type="submit" value="Enviar instrucciones")
                    .flex.items-center.justify-between.mt-5
                        a.text-neutral-950.text-xs(href="/auth/registro") ¿No tienes una cuenta todavía? Crea Una
                        a.text-neutral-950.text-xs(href="/auth/login") ¿Ya estás registrado? Inicia Sesión